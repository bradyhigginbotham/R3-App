function SearchWindow(navGroup){var ListView=require('ui/common/maps/search/ListView');var self=Ti.UI.createWindow({backgroundColor:'#ffffff',title:'Search',navBarHidden:false});var listView=new ListView();var listContainerWindow=Ti.UI.createWindow({title:'Search'});self.add(listView);listView.addEventListener('itemSelected',function(e){Ti.App.fireEvent('annotationSelected',{title:e.title});});var homeButton=Ti.UI.createButton({title:'Home'});self.leftNavButton=homeButton;homeButton.addEventListener('click',function(){navGroup.close(self.tabGroup);});return self;}
module.exports=SearchWindow;